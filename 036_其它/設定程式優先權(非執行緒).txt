設定程式優先權(非執行緒)
/////////////////////////////////////////
用
BOOL WINAPI SetPriorityClass(
  HANDLE hProcess,
  DWORD dwPriorityClass
);

如果還是不太了解
用SetPriorityClass到google去搜尋一下
就有範例可用
它有幾個等級可用，測試一下就可以發現不同處
//////////////////////////////////////////
原來是先用
HANDLE handle=GetCurrentProcess();
然後再使用SetPriorityClass就ok了
感謝大大 