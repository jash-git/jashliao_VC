test_createXML-->透過程式建立文件，並可以讀取修改標籤值和屬性值
test_Get_ChangeForm->取出標籤值並做資料拆解
test_MutiGetValue->單純主標籤名稱不同，但子標籤名稱相同，讀取修改標籤值；透過根節點依序取出子結點
test_read_msg->測試將XML的副檔名修改成msg來讀取裡面資料
多層且標籤名稱相同XML搜尋
ReadMultiLevelXML->取得目前路徑，有標準xml步驟，多層且標籤名稱相同XML搜尋