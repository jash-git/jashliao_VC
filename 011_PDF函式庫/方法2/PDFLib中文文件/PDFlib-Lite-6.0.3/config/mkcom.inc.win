# Template to be included from every makefile that creates COM DLL's
# $Id: mkcom.inc.win,v 1.2 2004/02/02 16:55:29 rjs Exp $
#
# Windows version
#

all::	$(LIBNAME).dll

$(LIBNAME).dll: $(OBJS)
	$(LD) $(COMDLLLDFLAG) /def:$(LIBNAME).def /out:$@ /implib:$(LIBNAME).lib $(OBJS) $(COMLDLIBS) $(LIBS)

clean::

distclean:: clean

depend:: .dummy

install::

uninstall::

test::

smoke::

.PHONY: clean

.dummy:

# suffixes
.SUFFIXES: .c .$(OBJ) .rc .res

.cpp$(LO):
	$(CC) -c $(CPPFLAGS) $(COMCFLAGS) $<

.rc.res:
	$(RC) $(RCFLAGS) $<
