memcpy(GetData.chrFirstData,m_v2,10); 
¨ä¤¤
	GetData.chrFirstData-------¦r¤¸°}¦C
	m_v2-------CString
/////////////////////////////////////////////////
CString Msg; 
     Msg=Msg+"abc"; 
     LPTSTR lpsz; 
     lpsz = new TCHAR[Msg.GetLength()+1]; 
     _tcscpy(lpsz, Msg); 
     char * psz; 
     strcpy(psz,lpsz);
/////////////////////////////////////////////////
char *ch; 
    CString str; 
    ch=(LPSTR)(LPCTSTR)str; 
 	